import { InjectionToken } from '@angular/core';

export const GBR_CONFIG_TOKEN = new InjectionToken<object>( 'It\'s just some notations...' );
export const GBR_ENVIRONMENT = new InjectionToken<object>( 'no matter what it is.....' );
