import { ElementInputDirective } from './element-input.directive';

describe('ElementInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementInputDirective();
    expect(directive).toBeTruthy();
  });
});
