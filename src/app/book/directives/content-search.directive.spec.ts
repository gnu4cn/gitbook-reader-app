import { ContentSearchDirective } from './content-search.directive';

describe('ContentSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentSearchDirective();
    expect(directive).toBeTruthy();
  });
});
